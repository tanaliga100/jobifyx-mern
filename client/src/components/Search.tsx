import { TextField } from "@mui/material";

const Search = () => {
  return (
    <TextField
      id="outlined-basic"
      size="small"
      placeholder="Search for the jobs"
      variant="outlined"
    />
  );
};

export default Search;
